#!/usr/bin/env python3
import ev3dev.ev3 as ev3
import time

class Sensors(object):
	def __init__ (self, angle, distance)
		self.angle = angle
		self.dictance = distance

	def read_angle(self, angle):
		// code

	def read_distance(self, distance):
		self.distance = us.units

def initialization():
	us = UltrasonicSensor()
	us.mode = 'US_DIST_CM'
	#units = us.units

def main():
	Analsys = Sensors(angle, distance)
	print (distance, '/n')

while (true):
	main()
